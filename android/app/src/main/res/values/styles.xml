<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Android <12 splash: legacy drawable (black bg + centred logo). -->
        <item name="android:windowBackground">@drawable/launch_background</item>

        <!-- Android 12+ splash (Splash Screen API). Solid black background
             + the transparent logo asset centered on screen. No branding
             image at the bottom — this is a logo-only splash.
             Note: the SplashScreen API renders the icon at ~192dp by
             default; a wider/taller PNG is scaled to fit. -->
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Theme for the uCrop activity used by image_cropper. The default
         Theme.AppCompat.Light.NoActionBar goes edge-to-edge on Android 15+,
         which puts the toolbar behind the status bar and the bottom
         controls behind the gesture bar. `fitsSystemWindows` insets the
         root layout so nothing overflows, and we match the status-bar
         colour to the toolbar (#14141A) + light-icon nav bar to the
         white bottom-controls surface for a seamless look. -->
    <style name="UCropTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">#14141A</item>
        <item name="android:navigationBarColor">#FFFFFF</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
