<?xml version="1.0" encoding="utf-8"?>
<!--
    Native Android launch screen (Android < 12).
    Black background + centred logoTransparent. The Android 12+ Splash
    Screen API overrides this with the values in styles.xml; this file
    is the fallback for older OS versions.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
