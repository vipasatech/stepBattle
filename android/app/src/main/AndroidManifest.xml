<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Step counting -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Background step sync.
         FOREGROUND_SERVICE (+ DATA_SYNC type for Android 14+) backs the
         flutter_foreground_task service that keeps syncing steps while a
         battle is active and the app is backgrounded. WAKE_LOCK +
         RECEIVE_BOOT_COMPLETED are declared by the plugin's manifest. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- Location. Coarse for the original home-district capture; fine +
         background for the Track (run/walk) feature so recording continues
         when the screen turns off. Background is gated behind an in-app
         prompt (PermissionService.requestRunPermissions) triggered only by
         tapping the Track FAB, so users who never use Track never see it. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>

    <!-- Health Connect permissions -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED"/>

    <!-- Health Connect package query -->
    <queries>
        <package android:name="com.google.android.apps.healthdata"/>
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
        </intent>
    </queries>

    <application
        android:label="StepBattle"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Required by Health Connect (Play Store version on Android <14):
                 the calling app must expose itself as a handler for the
                 permission-rationale intent, otherwise Health Connect rejects
                 every permission read with "is not declared!". -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
            </intent-filter>
        </activity>
        <!-- Health Connect permissions activity -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE"/>
                <category android:name="android.intent.category.HEALTH_PERMISSIONS"/>
            </intent-filter>
        </activity-alias>

        <!-- Background step-sync foreground service (flutter_foreground_task).
             Type is `dataSync` only — even though the Track session uses GPS,
             we deliberately DO NOT add the `location` FGS type here. The
             always-on service starts on login (before location permission
             has been granted), and Android 14+ rejects starting a service
             with `location` type when ACCESS_FINE_LOCATION isn't granted yet.
             The Track session's Geolocator stream works without the FGS type
             while the app has foreground state; truly background-with-screen-
             off GPS is Phase 2 (separate location service / plugin fork). -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Monochrome status-bar icon for the persistent live-stats
             notification. The key here is referenced from Dart via
             NotificationIcon(metaDataName: 'com.stepbattle.notification.icon')
             in lib/services/background_sync.dart. -->
        <meta-data
            android:name="com.stepbattle.notification.icon"
            android:resource="@drawable/ic_stat_logo" />

        <!-- Home-screen widget. Dart pushes data via HomeWidget.saveWidgetData
             + HomeWidget.updateWidget('StepBattleWidgetProvider'); this
             receiver re-renders the layout against the latest values. -->
        <receiver
            android:name="com.stepbattle.stepbattle.StepBattleWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/stepbattle_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
