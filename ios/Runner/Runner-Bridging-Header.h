// StepBattle iOS bridging header
// ================================
// Exposes Objective-C symbols to Swift. Currently only the Flutter plugin
// registrar is bridged — no custom native code requires bridging today.
//
// If you add a custom Obj-C or C library later (e.g. a native HealthKit
// helper or a Razorpay callback shim), import its main header here and it
// becomes visible to AppDelegate.swift and any Swift files under Runner/.

#import "GeneratedPluginRegistrant.h"
